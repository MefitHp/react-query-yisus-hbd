export const getCharactersSelector = (state) => state.characters.data;

export const getCharactersLoadingSelector = (state) => state.characters.loading;

export const getCharactersErrorSelector = (state) => state.characters.error;
